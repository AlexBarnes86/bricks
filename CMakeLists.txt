CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(TETRIS)
SET(EXECUTABLE_OUTPUT_PATH ${TETRIS_SOURCE_DIR}/bin)
INCLUDE_DIRECTORIES(${TETRIS_SOURCE_DIR}/include)
SET(CMAKE_CXX_FLAGS "-g -Wall")
ADD_SUBDIRECTORY(src)
